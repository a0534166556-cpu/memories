# MySQL Database Configuration (Local Development)
# Copy this file to .env and update with your local MySQL settings

# MySQL Connection Settings
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=memorial
MYSQL_PORT=3306

# Alternative environment variable names (Railway uses these)
# Uncomment and use these if you're using Railway or other cloud providers
# MYSQLHOST=localhost
# MYSQLUSER=root
# MYSQLPASSWORD=
# MYSQLDATABASE=memorial
# MYSQLPORT=3306

# JWT Secret for authentication (change this in production!)
JWT_SECRET=your-secret-key-change-in-production

# Server Port (optional - defaults to 8080)
PORT=8080

# Node Environment
NODE_ENV=development

# PayPal Configuration (optional - leave empty to disable payments)
# PAYPAL_CLIENT_ID=
# PAYPAL_CLIENT_SECRET=
# PAYPAL_MODE=sandbox

# Base URL for QR codes (for local development, use http://localhost:5173)
# For production, set to your Netlify URL
BASE_URL=http://localhost:5173

# אחסון קבוע (Railway): כדי שתמונות/וידאו/אודיו/QR לא יימחקו ב-deploy
# ב-Railway: צור Volume, מרכב על /app/data, והוסף משתנה STORAGE_PATH=/app/data
# STORAGE_PATH=

# Forgot password – שליחת אימייל לאיפוס סיסמה (אופציונלי)
# אם לא מוגדר: המשתמש יקבל הודעה לפנות ל־a0534166556@gmail.com
# אפשרות 1 – SendGrid: SENDGRID_API_KEY + RESET_EMAIL_FROM (כתובת מאומתת ב-SendGrid)
# SENDGRID_API_KEY=SG.xxxx
# RESET_EMAIL_FROM=noreply@yourdomain.com
# אפשרות 2 – SMTP (Gmail): SMTP_USER, SMTP_PASS, RESET_EMAIL_FROM
# SMTP_USER=your@gmail.com
# SMTP_PASS=your-app-password
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# RESET_EMAIL_FROM=noreply@yoursite.com

# תזכורת ביום הפטירה (יארצייט): קריאה יומית ל־GET /api/cron/send-yahrzeit-reminders?token=SECRET
# הגדר CRON_SECRET (מחרוזת סודית) והפעל cron יומי (למשל cron-job.org) עם ה-URL + token
# CRON_SECRET=your-secret-token

# --- אופציה: מקומי עם מסד הנתונים של Railway (בלי להרוס את האתר שפורסם) ---
# אם MySQL מקומי לא עובד: העתק מ-Railway (Variables) את:
#   MYSQL_HOST, MYSQL_USER, MYSQL_PASSWORD, MYSQL_DATABASE, MYSQL_PORT
# והדבק כאן. השרת המקומי יתחבר ל-Railway. האתר שפורסם ממשיך להשתמש בהגדרות של Railway.
