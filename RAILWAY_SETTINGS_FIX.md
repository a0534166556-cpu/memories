# 🔧 תיקון הגדרות Railway

## הבעיה

Railway מנסה להריץ `npm run build` מהשורש במקום להריץ את ה-backend.

## הפתרון

### שלב 1: לך ל-Settings של ה-Service

1. **לחץ על הכרטיס "memories"** במרכז המסך
2. **בצד ימין → לחץ על "Settings"**

### שלב 2: הגדר את ההגדרות הבאות

#### Root Directory:
```
backend
```
(רק `backend` - ללא `/`)

#### Start Command:
```
npm start
```
(או השאר ריק - Railway ישתמש ב-`npm start` אוטומטית)

#### Build Command:
**השאר ריק!** (או מחק אם יש משהו)

Railway לא צריך Build Command - הוא רק צריך להריץ `npm start`!

### שלב 3: שמור והפעל מחדש

1. **לחץ "Save"** או "Update"
2. **חזור ל-"Deployments"**
3. **לחץ "Redeploy"** או "Deploy the repo"

---

## ⚠️ חשוב

- **Root Directory:** חייב להיות `backend`
- **Start Command:** `npm start` (או ריק)
- **Build Command:** ריק! (אל תגדיר build command)

---

## אם עדיין לא עובד

### בדוק את ה-Logs:
1. **לך ל-"Logs"** ב-Railway
2. **קרא את השגיאה** - שלח לי ואעזור

---

**אחרי שתגדיר את זה נכון, זה אמור לעבוד!** 🚀


