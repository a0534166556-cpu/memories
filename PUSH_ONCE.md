# 🚀 העלה את הקוד ל-Railway - פעם אחת ולתמיד

## ✅ מה צריך לדחוף:

**רק קובץ אחד:**
```
backend/server.js
```

**זה הכל!** רק הקובץ הזה תיקנתי.

---

## 📝 איך לדחוף:

### שלב 1: פתח Terminal

פתח **Git Bash** או **PowerShell** בתיקיית הפרויקט:
```
C:\Users\a0534\OneDrive\שולחן העבודה\memories
```

### שלב 2: הרץ את הפקודות הבאות

העתק והדבק את הפקודות הבאות **אחת אחת**:

```bash
git add backend/server.js
```

```bash
git commit -m "Fix CORS - allow all origins"
```

```bash
git push
```

---

## ⚠️ חשוב:

- **זה לא ישפיע על Netlify!** Netlify עושה deploy רק מה-`frontend/`
- **רק Railway יעשה deploy** מה-`backend/`
- **זה יעבוד!** הקוד מאפשר את כל ה-origins

---

## ✅ אחרי הדחיפה:

1. **Railway יעשה Deploy אוטומטי** (תוך 1-2 דקות)
2. **לך ל-Railway → Logs** ותראה:
   - `Server running on port...`
   - `Memorials table ready`
   - `Candles table ready`
3. **רענן את הדף ב-Netlify** - הכל יעבוד!

---

**זה הכל! רק קובץ אחד, פעם אחת!** 🚀


