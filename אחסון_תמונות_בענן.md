# אחסון תמונות/וידאו/אודיו בענן (Cloud Storage)

אם תשמור **רק את הקבצים** (תמונות, וידאו, אודיו) בענן, הם לא ייעלמו בדיפלוי ולא תצטרך Volume ב-Railway. השרת רק מעלה קובץ ל־bucket ומשמור במסד הנתונים **קישור ציבורי** (URL).

---

## איך זה עובד היום

- הקבצים נשמרים על דיסק השרת (או על Volume ב-Railway).
- במסד נשמר **נתיב** כמו `/uploads/images/xxx.jpg`.
- Netlify מפנה `/uploads/*` ל־Railway, והפרונט מציג לפי הנתיב.

## איך זה יעבוד עם ענן

- בעת העלאה: השרת שולח את הקובץ ל־**שירות אחסון בענן** (S3, R2 וכו'), ומקבל **כתובת ציבורית**.
- במסד נשמר **ה-URL המלא**, למשל:  
  `https://your-bucket.r2.dev/memorials/abc-123.jpg`
- הפרונט כבר תומך ב־URL מלא (ב־`MemorialPage.jsx` – אם הנתיב מתחיל ב־`https://` הוא נשאר כפי שהוא).
- דיפלוי ב-Railway לא נוגע לקבצים – הם נשארים ב־bucket.

---

## שירותים מתאימים

| שירות | יתרונות |
|--------|---------|
| **Cloudflare R2** | תאימות S3, ללא egress fee, חינם עד גודל מסוים |
| **AWS S3** | נפוץ, תיעוד עשיר |
| **Backblaze B2** | זול, תאימות S3 |

---

## מה צריך לשנות בקוד (Backend)

1. **התקנת חבילה:** `@aws-sdk/client-s3` (עובד גם עם R2 – תאימות S3).
2. **משתני סביבה:**  
   `S3_BUCKET`, `S3_REGION`, `S3_ACCESS_KEY`, `S3_SECRET_KEY`,  
   ואם משתמשים ב־R2: `S3_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com`.
3. **לוגיקת העלאה:**  
   אחרי ש־multer מקבל את הקובץ (אפשר `memoryStorage` במקום דיסק), לשלוח ל־S3/R2 עם מפתח כמו `memorials/{memorialId}/{uuid}.jpg`, לקבל את ה־URL הציבורי (או לבנות אותו), ולשמור **את ה-URL** בעמודות `images` / `videos` / `heroImage` / `backgroundMusic` במסד (במקום `/uploads/...`).
4. **תאימות לאחור:**  
   אם במסד עדיין יש נתיבים כמו `/uploads/...`, להשאיר אותם – הפרונט כבר מטפל גם ב־URL מלא וגם בנתיב יחסי (עם ה-proxy ב-Netlify).

---

## סיכום

- **רק תמונות בענן** = העלאה ל־S3/R2, שמירת URL במסד. הפרונט לא חייב שינוי.
- **יתרונות:** אין תלות ב־Volume, קבצים שורדים דיפלוי, אפשר CDN/מטמון לפי ה-URL.
- **מחיר:** תלוי בשירות (R2 למשל חינם עד גודל ותעבורה מסוימים).

אם תרצה, אפשר לעבור צעד־אחר־צעד על שינוי קונקרטי ב־`server.js` (איפה לקרוא ל־S3, איפה לעדכן את המערך `images` וכו').
